#to define a function to use "def", followed by the function name
#all lines inside the function are executed
#IMPORTANT: the identation is important to identify what is part of that function 
#the first line like """...""" is for documentation of function

def sum():
    """Returns the sum of two numbers"""
    number1 = 1
    number2 = 2
    print(number1 + number2)

#If you execute a file, nothing will be executed
#because the function to need to be called
#to execute this defining.py, on the terminal type: py .\3_functions\defining.py
